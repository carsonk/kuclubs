extends layout.pug

block content
  .container.p-0.pt-3
    a.btn.btn-warning.float-right(href=`/club/${club.id}/edit`) Edit Club
    a.btn.btn-success.float-right.text-white(style='margin-right: 5px;') New Event
    h2.text-white= club.name
    .card
      .card-block
        .row
          .col-md-8
            li= club.description
            p!= club.photo_album_embed
          .col-md-4
            h5 Members
            ul
              each member in members
                li
                  = member.first_name + ' ' + member.last_name
                  if member.is_officer
                    span.officer-title= ' (' + member.officer_title + ')'
            a.btn.btn-outline-success.btn-sm(href=`/club/${club.id}/add`) Add Member
